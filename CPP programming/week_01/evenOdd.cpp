#include <iostream>
using namespace std;

int main () {

    int n;
    cin >> n;

    // for single if else, use it instead
    (n%2 == 0) ? cout << "Even" << endl : cout << "Odd" << endl;

    return 0;
}